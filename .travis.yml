language: php

php:
  - 7.0
  - 7.1
  - nightly

sudo: false

before_script:
  - composer self-update
  - composer install --prefer-dist;

script:
  - ./vendor/bin/parallel-lint ./src/
  - ./vendor/bin/phpcs -n -p --standard=PSR2 --extensions=php,twig ./src/
  - ./vendor/bin/php-cs-fixer fix -v ./src --level=psr2 --dry-run
  - ./vendor/bin/phpunit

notifications:
  email:
  - info@jield.nl

matrix:
  fast_finish: true