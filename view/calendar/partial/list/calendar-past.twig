<h1 class="display-4">{{ translate("txt-past-events") }}</h1>

<p class="lead">{{ translate("txt-past-events-explanation") }}</p>

{% for calendar in calendarItems if 'public' in calendar.type.access.toArray() %}
    <table class="table table-hover table-striped table-condensed table-sm">
        <tr>
            <th colspan="2">{{ calendarLink(calendar,'view','name') }}</th>
        </tr>
        <tr>
            <td style="width: 200px">{{ translate("txt-date") }}</td>
            <td>{{ calendar.dateFrom|from_to_date(calendar.dateEnd) }}</td>
        </tr>
        {% if calendar.location %}
            <tr>
                <td>{{ translate("txt-location") }}</td>
                <td>{{ calendar.location }}</td>
            </tr>
        {% endif %}
        {% if calendar.description %}
            <tr>
                <td>{{ translate("txt-more-information") }}</td>
                <td>{{ calendar.description|raw }}</td>
            </tr>
        {% endif %}
        {% if calendar.url %}
            <tr>
                <td>{{ translate("txt-link") }}</td>
                <td><a href="{{ calendar.url }}" target="_blank">{{ calendar.url }}</a></td>
            </tr>
        {% endif %}
    </table>
    <hr>
    <br>
{% endfor %}
